{
  "name": "al-rasikhoon-firestore-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security-rules unit tests (privilege-boundary coverage for #28 supervisor scoping).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project=alrasikhoon-57151 \"mocha firestore.rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "mocha": "^10.4.0"
  }
}
