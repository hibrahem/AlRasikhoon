# Container Diagram — AlRasikhoon

> **C4 Level 2** — the system broken down into deployable/runnable containers. Audience: the dev team.

## Diagram

```mermaid
C4Container
    title Container Diagram for AlRasikhoon

    Person(user, "User", "Student, teacher, supervisor, or admin")

    System_Boundary(boundary, "AlRasikhoon") {
        Container(app, "Mobile & Web App", "Flutter / Dart", "Android, iOS & Web client. Riverpod state, go_router navigation, Arabic-first i18n.")
        Container(functions, "Cloud Functions", "Node.js 20 / TypeScript (Firebase Functions v2)", "Role-based user provisioning (onCall) + role custom-claim sync (Firestore trigger).")
        ContainerDb(db, "Firestore Database", "Cloud Firestore", "Application data. Security rules enforce role-based access.")
    }

    System_Ext(auth, "Firebase Authentication", "Email/password + custom 'role' claim")
    System_Ext(storage, "Firebase Storage", "Media / asset storage")

    Rel(user, app, "Uses", "iOS / Android / Web")
    Rel(app, auth, "Authenticates", "Firebase Auth SDK")
    Rel(app, db, "Reads / writes (role-gated)", "Firestore SDK")
    Rel(app, functions, "Calls", "HTTPS callable")
    Rel(app, storage, "Uploads / downloads", "Storage SDK")
    Rel(functions, auth, "Creates users, sets role claims", "Admin SDK")
    Rel(functions, db, "Writes profiles / syncs claims", "Admin SDK")
    Rel(db, functions, "onDocumentWritten trigger", "Firestore trigger")
```

> **Note**: this diagram was auto-generated by /handover on 2026-05-17 from repo signals (pubspec.yaml, firebase.json, functions/package.json, functions/src/index.ts, firestore.rules). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector may have picked a framework version wrong
> - Inferred relationships — `user → app` assumes the standard Firebase client SDKs; adjust if your stack uses something else
> - External systems — anything used that isn't in pubspec.yaml / package.json (e.g. Firebase Hosting serving `build/web`, App Check, analytics) won't have been detected
>
> Update the "Maintenance" section below once the diagram is stable.

## Maintenance

(From the template — update when L2 containers change.)
