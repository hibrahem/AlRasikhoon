<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override that permits cleartext HTTP to the host machine when
  running on the Android emulator. Required for connecting to the Firebase
  Emulator Suite (Auth on 9099, Firestore on 8080) over `10.0.2.2`.

  This file lives under `src/debug/`, so it is not included in release builds.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
